export enum ItemType {
  TEXT = "text",
  LINK = "link",
  TODO = "todo",
  REMINDER = "reminder",
}

export enum ViewWidthType {
  DEFAULT = "default",
  COMPACT = "compact",
  FLOW = "flow",
  EXPANDED = "expanded",
}

export enum ViewMarginsType {
  DEFAULT = "default",
  COMPACT = "compact",
}
